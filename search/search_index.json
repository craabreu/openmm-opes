{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"openmm-opes","text":"<p>An OpenMM implementation of On-the-fly Probability Enhanced Sampling (OPES) and its exploratory variant, OPES-explore.</p> <p>OPES builds a bias potential from an on-the-fly estimate of the probability distribution along a set of collective variables, rather than accumulating repulsive hills the way metadynamics does. It needs only three parameters: the deposition pace, the initial kernel bandwidth, and the approximate height of the barrier to overcome.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install openmm-opes\n</code></pre> <p>OpenMM itself is also distributed through conda-forge, if you prefer conda environments:</p> <pre><code>mamba install -c conda-forge openmm\npip install openmm-opes\n</code></pre>"},{"location":"#at-a-glance","title":"At a glance","text":"<pre><code>from openmm import app\nfrom openmm_opes import OPES\n\nsampler = OPES(\n    system, [phi, psi], 300 * unit.kelvin, 40 * unit.kilojoules_per_mole,\n    frequency=500, varianceFrequency=50,\n)\nsampler.step(simulation, 1_000_000)\nfes = sampler.getFreeEnergy()\n</code></pre> <p>See the quickstart for a complete runnable example, and the theory page for how the code maps onto the published equations.</p>"},{"location":"#origin","title":"Origin","text":"<p>This library packages and hardens the prototype implementation developed in craabreu/opes-simulations, porting its <code>OPES</code>/<code>OnlineKDE</code> classes into a tested, documented package.</p>"},{"location":"#references","title":"References","text":"<ol> <li>Invernizzi &amp; Parrinello, Rethinking Metadynamics: From Bias Potentials to    Probability Distributions, J. Phys. Chem. Lett. 2020.    doi:10.1021/acs.jpclett.0c00497</li> <li>Invernizzi &amp; Parrinello, Exploration vs Convergence Speed in Adaptive-Bias    Enhanced Sampling, J. Chem. Theory Comput. 2022.    doi:10.1021/acs.jctc.2c00152</li> </ol>"},{"location":"api/","title":"API reference","text":""},{"location":"api/#openmm_opes.opes.OPES","title":"OPES","text":"<pre><code>OPES(system, variables, temperature, barrier, frequency, varianceFrequency, biasFactor=None, exploreMode=False, bounded=False, saveFrequency=None, biasDir=None, walkerId=None, warmupSteps=None, compressionThreshold=1.0, useExistingBandwidths=True, kernelShape='gaussian', statsWindowSize=10)\n</code></pre> <p>On-the-fly Probability Enhanced Sampling.</p> <p>Implements OPES and its exploratory variant, following Invernizzi and Parrinello, https://doi.org/10.1021/acs.jpclett.0c00497 and https://doi.org/10.1021/acs.jctc.2c00152.</p> <p>Parameters:</p> Name Type Description Default <code>system</code> <p>The System to simulate. A CustomCVForce implementing the bias is created and added to it.</p> required <code>variables</code> <p>The collective variables whose sampling should be enhanced.</p> required <code>temperature</code> <p>The temperature at which the simulation is run.</p> required <code>barrier</code> <p>The free energy barrier the simulation should overcome.</p> required <code>frequency</code> <p>Interval in time steps at which to deposit a kernel.</p> required <code>varianceFrequency</code> <p>Interval in time steps at which to update the CV variance. When None, the bandwidth is fixed and taken from each variable's <code>biasWidth</code>, which is interpreted as the standard deviation of the UNBIASED distribution, exactly as both papers define sigma^(0).</p> required <code>biasFactor</code> <p>Defaults to <code>barrier / kT</code>. Must be finite: unlike PLUMED, the uniform-target limit <code>inf</code> is not supported.</p> <code>None</code> <code>exploreMode</code> <code>bool</code> <p>Whether to apply the OPES-explore variant. Must match the mode of a snapshot passed to :meth:<code>setState</code> and of every walker sharing <code>biasDir</code>.</p> <code>False</code> <code>bounded</code> <code>bool</code> <p>Whether non-periodic CVs have reflective boundaries.</p> <code>False</code> <code>saveFrequency</code> <p>Interval in time steps at which to share the bias on disk. Must be a multiple of <code>frequency</code>.</p> <code>None</code> <code>biasDir</code> <p>Directory shared with other walkers.</p> <code>None</code> <code>walkerId</code> <p>This walker's identifier within <code>biasDir</code>. Random when omitted; pass a stable value across restarts so a resumed walker reclaims its own file rather than leaving the previous run's behind as a phantom peer.</p> <code>None</code> <code>warmupSteps</code> <p>When given, run this many steps without depositing kernels while measuring the CV variance, then freeze it.</p> <code>None</code> <code>compressionThreshold</code> <code>float</code> <p>Mahalanobis distance below which kernels are merged.</p> <code>1.0</code> <code>useExistingBandwidths</code> <code>bool</code> <p>Whether the merge search scales by existing kernels' bandwidths.</p> <code>True</code> <code>kernelShape</code> <code>str</code> <p><code>\"gaussian\"</code> or <code>\"compact\"</code>.</p> <code>'gaussian'</code> <code>statsWindowSize</code> <code>int</code> <p>Window, in deposition strides, of the running CV-mean estimate. With an adaptive variance, the first kernel also waits this many strides, so that it is sized from a measured variance.</p> <code>10</code>"},{"location":"api/#openmm_opes.opes.OPES.addKernel","title":"addKernel","text":"<pre><code>addKernel(values, biasEnergy, variance=None)\n</code></pre> <p>Deposit a kernel into the probability estimates.</p> <p>Returns whether a kernel was actually deposited: a non-positive variance estimate is skipped rather than deposited, since a zero bandwidth gives the kernel -inf log height and poisons the estimate.</p> <p>This does not refresh any Context; call :meth:<code>updateContext</code> afterwards if a simulation is running.</p>"},{"location":"api/#openmm_opes.opes.OPES.getAverageDensity","title":"getAverageDensity","text":"<pre><code>getAverageDensity()\n</code></pre> <p>Z_n, the mean density over the explored CV space.</p> <p>NaN until the first kernel is deposited, like :meth:<code>getFreeEnergy</code>.</p>"},{"location":"api/#openmm_opes.opes.OPES.getBias","title":"getBias","text":"<pre><code>getBias()\n</code></pre> <p>The OPES bias potential on the grid.</p> <p>With no kernels deposited yet the estimate is empty, and both the log PDF and the log mean density are -inf, whose difference is NaN. The bias is well defined in that limit, though: the regularization term dominates, leaving the flat -barrier floor. Returning it explicitly keeps NaN out of the forces.</p>"},{"location":"api/#openmm_opes.opes.OPES.getCollectiveVariables","title":"getCollectiveVariables","text":"<pre><code>getCollectiveVariables(simulation)\n</code></pre> <p>Current values of all collective variables in a Simulation.</p>"},{"location":"api/#openmm_opes.opes.OPES.getFreeEnergy","title":"getFreeEnergy","text":"<pre><code>getFreeEnergy()\n</code></pre> <p>Free energy as a function of the collective variables.</p> <p>Returned as an N-dimensional array in kJ/mole. The i'th position along an axis corresponds to <code>minValue + i*(maxValue-minValue)/(gridWidth-1)</code>, matching the <code>numpy.linspace(minValue, maxValue, gridWidth)</code> axis the estimate is built on. Always the importance-sampling estimate, which converges better than the direct one in explore mode.</p> <p>Every entry is NaN until the first kernel is deposited: with no samples there is no density to take a logarithm of. Unlike :meth:<code>getBias</code>, this has no defined limit to fall back on, and it never reaches the forces.</p>"},{"location":"api/#openmm_opes.opes.OPES.getNumKernels","title":"getNumKernels","text":"<pre><code>getNumKernels()\n</code></pre> <p>Number of kernels in the estimate that defines the bias.</p>"},{"location":"api/#openmm_opes.opes.OPES.getState","title":"getState","text":"<pre><code>getState()\n</code></pre> <p>Flat, npz-writable snapshot of this sampler's accumulated state.</p>"},{"location":"api/#openmm_opes.opes.OPES.getVariance","title":"getVariance","text":"<pre><code>getVariance()\n</code></pre> <p>Variance currently used to size newly deposited kernels.</p>"},{"location":"api/#openmm_opes.opes.OPES.setState","title":"setState","text":"<pre><code>setState(state)\n</code></pre> <p>Restore from a :meth:<code>getState</code> snapshot.</p> <p>A snapshot from a single-walker run seeds this walker's own contribution to the shared bias, since that run had no peers.</p> <p>The restored bias reaches the simulation at the start of the next :meth:<code>step</code>. Call :meth:<code>updateContext</code> to push it into a Context sooner, e.g. before computing energies.</p>"},{"location":"api/#openmm_opes.opes.OPES.step","title":"step","text":"<pre><code>step(simulation, steps)\n</code></pre> <p>Advance the simulation by a number of time steps.</p> <p>Parameters:</p> Name Type Description Default <code>simulation</code> <p>The Simulation to advance.</p> required <code>steps</code> <p>The number of time steps to integrate.</p> required"},{"location":"api/#openmm_opes.opes.OPES.updateContext","title":"updateContext","text":"<pre><code>updateContext(context)\n</code></pre> <p>Push the current bias into a Context.</p> <p>The table stores V + barrier, so the energy the force contributes is the bias shifted up by <code>barrier</code>: the unexplored floor sits at 0. OpenMM's tabulated functions are zero outside their range, so a CV that leaves <code>[minValue, maxValue]</code> sees the bias of a region nothing has been deposited in, rather than a jump of <code>barrier</code>.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE","title":"OnlineKDE","text":"<pre><code>OnlineKDE(cvSpace, compressionThreshold=1.0, useExistingBandwidths=True, kernelShape='gaussian')\n</code></pre> <p>Online kernel density estimate with on-the-fly kernel compression.</p> <p>Parameters:</p> Name Type Description Default <code>cvSpace</code> <p>The :class:<code>CVSpace</code> the estimate lives on.</p> required <code>compressionThreshold</code> <code>float</code> <p>Merge a new kernel into any existing kernel closer than this, measured as a Mahalanobis distance. Zero disables merging.</p> <code>1.0</code> <code>useExistingBandwidths</code> <code>bool</code> <p>Whether the nearest-neighbour search scales distances by the existing kernels' bandwidths rather than the incoming kernel's.</p> <code>True</code> <code>kernelShape</code> <code>str</code> <p>Either <code>\"gaussian\"</code> or <code>\"compact\"</code>.</p> <code>'gaussian'</code>"},{"location":"api/#openmm_opes.kde.OnlineKDE.__iadd__","title":"__iadd__","text":"<pre><code>__iadd__(other)\n</code></pre> <p>Absorb every kernel of <code>other</code>.</p> <p>The weight moments are combined directly rather than re-accumulated from the absorbed kernels: a compressed kernel carries the combined weight of all its samples, so squaring it would overstate the sum of squared weights and collapse the effective sample size.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.bandwidthFactor","title":"bandwidthFactor","text":"<pre><code>bandwidthFactor(logWeight)\n</code></pre> <p>Silverman shrink factor for a new kernel of the given log weight.</p> <p>Computed from this estimate's effective sample size with the new kernel's weight already counted, which is what :meth:<code>update</code> applies.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.evaluate","title":"evaluate","text":"<pre><code>evaluate(point)\n</code></pre> <p>Log of the normalized density at a single point.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.getLogMeanDensity","title":"getLogMeanDensity","text":"<pre><code>getLogMeanDensity()\n</code></pre> <p>Log of Z_n, the mean density over the compressed kernel centers.</p> <p>NaN with no kernels deposited yet, for the same reason as :meth:<code>getLogPDF</code>: the mean over an empty set is undefined, so the log(0) and inf - inf along the way are expected, not warned about.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.getLogPDF","title":"getLogPDF","text":"<pre><code>getLogPDF()\n</code></pre> <p>Log of the normalized probability density on the grid.</p> <p>With no kernels deposited yet, both operands are -inf and the result is the documented NaN (see :meth:<code>getState</code>'s empty-KDE note) rather than an error; the subtraction is expected to be undefined here, not a sign that something went wrong.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.getNumKernels","title":"getNumKernels","text":"<pre><code>getNumKernels()\n</code></pre> <p>Number of compressed kernels currently stored.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.getState","title":"getState","text":"<pre><code>getState()\n</code></pre> <p>Flat, npz-writable snapshot of this estimate.</p> <p>The CVSpace is deliberately excluded: all walkers share identical CV definitions, so a reader constructs its own and calls setState.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.setState","title":"setState","text":"<pre><code>setState(state)\n</code></pre> <p>Restore from a :meth:<code>getState</code> snapshot, discarding current contents.</p>"},{"location":"api/#openmm_opes.kde.OnlineKDE.update","title":"update","text":"<pre><code>update(position, logWeight, variance, factor=None)\n</code></pre> <p>Deposit a kernel of the given log weight and per-CV variance.</p> <p>The bandwidth is shrunk by <code>factor</code> when given, and otherwise by this estimate's own :meth:<code>bandwidthFactor</code>. Passing one lets an estimate that is part of a larger one size kernels by the larger one's sample size.</p>"},{"location":"api/#openmm_opes.kde.CVSpace","title":"CVSpace","text":"<pre><code>CVSpace(variables, bounded=False)\n</code></pre> <p>The domain of the collective variables: grid, periodicity, boundaries.</p> <p>Parameters:</p> Name Type Description Default <code>variables</code> <p>Sequence of collective variable descriptions. Each must expose <code>minValue</code>, <code>maxValue</code>, <code>gridWidth</code> and <code>periodic</code>; an <code>openmm.app.BiasVariable</code> qualifies.</p> required <code>bounded</code> <code>bool</code> <p>Whether non-periodic variables have reflective boundaries. When true the internal grid is tripled with mirrored copies on each side, and :meth:<code>foldedGrid</code> folds them back.</p> <code>False</code>"},{"location":"api/#openmm_opes.kde.CVSpace.gridShape","title":"gridShape  <code>property</code>","text":"<pre><code>gridShape\n</code></pre> <p>Shape of the CV-space grid, slowest axis first.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.numDimensions","title":"numDimensions  <code>property</code>","text":"<pre><code>numDimensions\n</code></pre> <p>Number of collective variables.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.closestNode","title":"closestNode","text":"<pre><code>closestNode(position)\n</code></pre> <p>Index of the grid node nearest to <code>position</code>, in gridShape order.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.displacement","title":"displacement","text":"<pre><code>displacement(position, endpoint)\n</code></pre> <p>Displacement from <code>position</code> to <code>endpoint</code>, minimum-image if periodic.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.endpoint","title":"endpoint","text":"<pre><code>endpoint(position, displacement)\n</code></pre> <p>Endpoint reached from <code>position</code>, wrapped into the periodic domain.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.foldedGrid","title":"foldedGrid","text":"<pre><code>foldedGrid(values)\n</code></pre> <p>Fold a tripled bounded grid back onto the physical domain, in log space.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.gridDistances","title":"gridDistances","text":"<pre><code>gridDistances(position)\n</code></pre> <p>Per-axis distances from <code>position</code> to every grid node.</p>"},{"location":"api/#openmm_opes.kde.CVSpace.mirrorPositions","title":"mirrorPositions","text":"<pre><code>mirrorPositions(position)\n</code></pre> <p>Every image of <code>position</code> under the reflective boundaries.</p> <p>For each non-periodic dimension, reflecting a point off both walls gives two images in addition to the point itself; the images across every dimension combine multiplicatively (a corner sees images of images). Unbounded spaces, or spaces with only periodic CVs, have no images: <code>[position]</code> is returned unchanged.</p> <p>A kernel centered at any one of these images is, by the symmetry of reflection, exactly as far from a given query point as the original kernel is from that point's own mirror image -- so summing a kernel's density over these images is equivalent to summing the query point's images against the one true kernel, which is what the tripled evaluation grid does. Used to keep :meth:<code>Kernel.evaluate</code> (arbitrary points) consistent with :meth:<code>Kernel.evaluateOnGrid</code>.</p>"},{"location":"api/#openmm_opes.kde.Kernel","title":"Kernel","text":"<pre><code>Kernel(cvSpace, position, bandwidth, logWeight, numSamples=1, shape=GAUSSIAN)\n</code></pre> <p>A multivariate kernel with diagonal covariance.</p>"},{"location":"api/#openmm_opes.kde.Kernel.evaluate","title":"evaluate","text":"<pre><code>evaluate(points)\n</code></pre> <p>Log of the kernel at the given point or points.</p> <p>Reflected at the domain walls when the space is bounded, by summing over the kernel's mirror images (see :meth:<code>CVSpace.mirrorPositions</code>) so this agrees with :meth:<code>evaluateOnGrid</code>. Unbounded spaces have no images, so this reduces to the single-term evaluation.</p>"},{"location":"api/#openmm_opes.kde.Kernel.evaluateOnGrid","title":"evaluateOnGrid","text":"<pre><code>evaluateOnGrid()\n</code></pre> <p>Log of the kernel on the CV-space grid, in gridShape order.</p>"},{"location":"api/#openmm_opes.kde.Kernel.findNearest","title":"findNearest","text":"<pre><code>findNearest(centers, bandwidths, ignore=())\n</code></pre> <p>Index of and squared Mahalanobis distance to the nearest center.</p>"},{"location":"api/#openmm_opes.kde.Kernel.merge","title":"merge","text":"<pre><code>merge(other)\n</code></pre> <p>Absorb <code>other</code>, preserving total weight, mean and second moment.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>A single particle on a two-dimensional M\u00fcller-Brown potential, biased along <code>x</code> and <code>y</code>, recovering the free energy surface.</p> <pre><code>import numpy as np\nimport openmm as mm\nfrom openmm import app, unit\n\nfrom openmm_opes import OPES\n\nKB = unit.MOLAR_GAS_CONSTANT_R.value_in_unit(\n    unit.kilojoules_per_mole / unit.kelvin\n)\n\nmuller_brown = (\n    \"-200*exp(-(x-1)^2-10*y^2)\"\n    \" -100*exp(-x^2-10*(y-0.5)^2)\"\n    \" -170*exp(-6.5*(0.5+x)^2+11*(x+0.5)*(y-1.5)-6.5*(y-1.5)^2)\"\n    \" +15*exp(0.7*(1+x)^2+0.6*(x+1)*(y-1)+0.7*(y-1)^2)\"\n)\n\nsystem = mm.System()\nsystem.addParticle(1.0)\npotential = mm.CustomExternalForce(muller_brown)\npotential.addParticle(0, [])\nsystem.addForce(potential)\n\nvariables = []\nfor expression, lo, hi in ((\"x\", -1.5, 1.2), (\"y\", -0.2, 2.0)):\n    force = mm.CustomExternalForce(expression)\n    force.addParticle(0, [])\n    variables.append(app.BiasVariable(force, lo, hi, 0.1, False, 101))\n\ntemperature = 1.0 / KB * unit.kelvin\nsampler = OPES(\n    system,\n    variables,\n    temperature,\n    barrier=20 * unit.kilojoules_per_mole,\n    frequency=500,\n    varianceFrequency=50,\n)\n\ntopology = app.Topology()\ntopology.addAtom(\"P\", None, topology.addResidue(\"MOL\", topology.addChain()))\nintegrator = mm.LangevinMiddleIntegrator(\n    temperature, 10.0 / unit.picosecond, 0.005 * unit.picoseconds\n)\nsimulation = app.Simulation(\n    topology, system, integrator, mm.Platform.getPlatformByName(\"Reference\")\n)\nsimulation.context.setPositions([mm.Vec3(-0.5, 1.4, 0.0)])\nsimulation.context.setVelocitiesToTemperature(temperature)\n\nsampler.step(simulation, 2_000_000)\n\nfes = sampler.getFreeEnergy().value_in_unit(unit.kilojoules_per_mole)\nfes -= fes.min()\nprint(f\"{sampler.getNumKernels()} kernels, barrier {fes.max():.1f} kJ/mol\")\n</code></pre> <p><code>getFreeEnergy()</code> returns an array whose shape is the CV grid with the last variable varying fastest, so for two CVs it has shape <code>(ny, nx)</code> and can be handed straight to <code>matplotlib.pyplot.contourf</code>.</p>"},{"location":"quickstart/#choosing-the-bandwidth","title":"Choosing the bandwidth","text":"<p>Both papers prescribe measuring the initial bandwidth from a short unbiased run. You can let the sampler do that for you:</p> <pre><code>sampler = OPES(..., varianceFrequency=50, warmupSteps=50_000)\n</code></pre> <p>Nothing is deposited during the first 50,000 steps; the CV variance measured there becomes the initial bandwidth and is then held fixed.</p> <p>To fix the bandwidth yourself instead, pass <code>varianceFrequency=None</code> and set each variable's <code>biasWidth</code>. It is read as \\(\\sigma^{(0)}\\), the standard deviation of the unbiased distribution, in both modes. OPES-explore kernels estimate the sampled distribution, which is \\(\\sqrt{\\gamma}\\) times wider, so they are deposited \\(\\sqrt{\\gamma}\\) times wider than <code>biasWidth</code>.</p>"},{"location":"theory/","title":"The method as implemented","text":"<p>Notation follows Invernizzi and Parrinello. \\(\\beta = 1/k_BT\\), \\(\\gamma\\) is the bias factor, and \\(\\mathbf{s}\\) the collective variables.</p>"},{"location":"theory/#the-probability-estimate","title":"The probability estimate","text":"<p>OPES estimates the unbiased distribution by weighted kernel density estimation,</p> \\[P_n(\\mathbf{s}) = \\frac{\\sum_k^n w_k\\, G(\\mathbf{s}, \\mathbf{s}_k)}{\\sum_k^n w_k}, \\qquad w_k = e^{\\beta V_{k-1}(\\mathbf{s}_k)}\\] <p>with Gaussian kernels of fixed height \\(h = \\prod_i (\\sigma_i\\sqrt{2\\pi})^{-1}\\). This is <code>OnlineKDE.getLogPDF()</code>; the weight is the bias energy read from the OPES force group before the new kernel is deposited, so it really is \\(V_{k-1}\\).</p>"},{"location":"theory/#bandwidth","title":"Bandwidth","text":"<p>Bandwidths shrink as the effective sample size \\(N_{\\text{eff}} = (\\sum_k w_k)^2 / \\sum_k w_k^2\\) grows, by Silverman's rule:</p> \\[\\sigma_i^{(n)} = \\sigma_i^{(0)}\\left[N_{\\text{eff}}^{(n)}(d+2)/4\\right]^{-1/(d+4)}\\]"},{"location":"theory/#normalization","title":"Normalization","text":"<p>\\(Z_n\\) normalizes over the CV space explored so far, and is approximated by a sum over the compressed kernel centers \u2014 <code>OnlineKDE.getLogMeanDensity()</code>.</p> \\[Z_n = \\frac{1}{|\\Omega_n|}\\int_{\\Omega_n} P_n(\\mathbf{s})\\, d\\mathbf{s}\\]"},{"location":"theory/#the-bias","title":"The bias","text":"\\[V_n(\\mathbf{s}) = \\left(1 - \\tfrac{1}{\\gamma}\\right)\\frac{1}{\\beta} \\log\\left(\\frac{P_n(\\mathbf{s})}{Z_n} + \\epsilon\\right)\\] <p>with \\(\\epsilon = e^{-\\beta\\Delta E/(1-1/\\gamma)}\\) limiting the bias to the barrier \\(\\Delta E\\). This is <code>OPES.getBias()</code>.</p>"},{"location":"theory/#opes-explore","title":"OPES-explore","text":"<p>The explore variant estimates the sampled distribution instead, with uniform weights,</p> \\[p^{\\text{WT}}_n(\\mathbf{s}) = \\frac{1}{n}\\sum_k^n G(\\mathbf{s}, \\mathbf{s}_k), \\qquad V_n(\\mathbf{s}) = (\\gamma-1)\\frac{1}{\\beta} \\log\\left(\\frac{p^{\\text{WT}}_n(\\mathbf{s})}{Z_n} + \\epsilon\\right)\\] <p>It explores faster and converges more slowly. <code>getBias()</code> reads the unweighted estimate in explore mode and the reweighted one otherwise. Because \\(p^{\\text{WT}}\\) is about \\(\\sqrt{\\gamma}\\) times wider than \\(P\\), explore kernels start at \\(\\sqrt{\\gamma}\\,\\sigma^{(0)}\\), with a fixed <code>biasWidth</code> still read as the unbiased \\(\\sigma^{(0)}\\).</p>"},{"location":"theory/#free-energy","title":"Free energy","text":"<p><code>getFreeEnergy()</code> always uses the importance-sampling estimate, \\(F_n = -\\beta^{-1}\\log P_n\\). In standard OPES the direct and reweighted routes are equivalent; in explore mode they differ until convergence, and the reweighted one converges better.</p>"},{"location":"theory/#kernel-compression","title":"Kernel compression","text":"<p>Rather than storing a bias grid, kernels closer than <code>compressionThreshold</code> in Mahalanobis distance are merged, preserving total weight, mean and second moment. The number of compressed kernels is what makes \\(|\\Omega_n|\\) estimable.</p>"},{"location":"theory/#warm-up-bandwidth-estimation","title":"Warm-up bandwidth estimation","text":"<p>Both papers prescribe measuring \\(\\sigma^{(0)}\\) from a short unbiased run. Passing <code>warmupSteps</code> automates this: no kernels are deposited until that many steps have elapsed, so the CV variance measured over that span is genuinely unbiased, and it is frozen (scaled by \\(\\gamma\\), since kernels are built from a sampled-distribution variance by convention) once warm-up ends.</p>"}]}